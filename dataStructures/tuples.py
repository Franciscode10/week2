#We use tuples when we want to store just two or three values together like a point in the space for example

point = (12.5, 10.6)
print(type(point))

list = [1,2,3,4]
Tuple = tuple(list)
print(f"This is a tuple {Tuple}")

print(Tuple[0])
print(Tuple[2])
print(Tuple[-1])